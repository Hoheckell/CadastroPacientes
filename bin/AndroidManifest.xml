<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unifor.programacao.paciente"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="16" />

    <!-- Inclusão de solicitações de acesso ao dispositivo. -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_MMS" />
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".atividade.PrincipalAtividade"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".atividade.ListaPacienteAtividade"
            android:label="@string/title_activity_lista_paciente_atividade" >
        </activity>
        <activity
            android:name=".atividade.PacienteAtividade"
            android:label="@string/title_activity_paciente_atividade" >
        </activity>
        <activity
            android:name=".atividade.RegistroPacienteAtividade"
            android:label="@string/title_activity_registro_paciente_atividade" >
        </activity>
        <activity
            android:name="br.unifor.programacao.paciente.atividade.InformacoesAdicionaisAtividade"
            android:label="@string/title_activity_informacoes_adicionais" >
        </activity>
    </application>

</manifest>